# Dependencies
node_modules

# Next.js
.next
out

# Env — ignore all local env files (backups, dumps, vercel pulls); keep the example tracked
.env*
!.env.example

# Misc
dist
*.log
.DS_Store
.vercel
.env.production

# Yarn Berry install cache (regenerated on every install)
.yarn/install-state.gz

# TypeScript incremental build cache (regenerated on every tsc run)
tsconfig.tsbuildinfo

# MCP docs index (generated at build/CI from rules/*.md by scripts/generate-mcp-docs-index.mjs)
src/lib/mcp/docs-index.generated.json
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
